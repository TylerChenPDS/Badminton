<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="njit.dao.ResourceDao">
	<select id="isExitByPath" resultType="int">
		select count(id) from t_resource where path=#{path}
	</select>
    
</mapper>